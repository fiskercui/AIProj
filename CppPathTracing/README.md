# C++ 路径追踪渲染器

一个简单易懂的 C++ 离线路径追踪渲染器，适合学习计算机图形学基础。

## 🎯 项目特点

- ✨ 纯 C++ 实现，不依赖复杂的图形 API
- 📚 代码清晰，易于理解和学习
- 🎨 支持多种材质（漫反射、金属、玻璃）
- 🖼️ 输出 PPM/PNG 格式图像

## 🛠️ 技术栈

- **语言**: C++17
- **构建系统**: CMake 3.10+
- **依赖**: stb_image_write.h (单头文件库)

## 📦 构建步骤

### Windows (使用 Visual Studio)

```bash
# 1. 创建构建目录
mkdir build
cd build

# 2. 生成 Visual Studio 项目
cmake ..

# 3. 编译项目
cmake --build . --config Release

# 4. 运行
bin\Release\path_tracer.exe
```

### Windows (使用 MinGW)

```bash
# 1. 创建构建目录
mkdir build
cd build

# 2. 生成 Makefile
cmake -G "MinGW Makefiles" ..

# 3. 编译
cmake --build .

# 4. 运行
bin\path_tracer.exe
```

## 📁 项目结构

```
CppPathTracing/
├── src/                    # 源代码目录
│   ├── main.cpp           # 主程序入口
│   ├── vec3.h             # 3D 向量数学库
│   ├── ray.h              # 光线类
│   ├── camera.h           # 相机类
│   ├── hittable.h         # 可碰撞对象接口
│   ├── sphere.h           # 球体类
│   ├── hittable_list.h    # 场景对象列表
│   ├── material.h         # 材质系统
│   └── utils.h            # 工具函数
├── external/              # 第三方库
│   └── stb_image_write.h  # 图像输出库
├── output/                # 渲染结果输出目录
├── CMakeLists.txt         # CMake 配置
└── README.md              # 项目说明
```

## 🎨 渲染示例

运行程序后，会在 `output` 目录下生成渲染图像：
- `output/render.ppm` - PPM 格式（文本格式，易于调试）
- `output/render.png` - PNG 格式（压缩格式）

## 📚 学习资源

- [Ray Tracing in One Weekend](https://raytracing.github.io/)
- [Scratchapixel](https://www.scratchapixel.com/)

## 🚀 开发路线

- [x] 项目结构搭建
- [ ] 向量数学库
- [ ] 光线系统
- [ ] 基础渲染输出
- [ ] 场景对象（球体）
- [ ] 材质系统
- [ ] 抗锯齿优化

## 📝 许可证

MIT License - 自由使用和学习